---
title: Colors
---
    
<ul class="layout-flex" role="list">
    {% for colorToken in colors %} 
    <li>
        <figure class="swatch">
            <figcaption class="background-{{ colorToken.name }}">
                <strong>{{ colorToken.name }}</strong> {{ colorToken.color }}
            </figcaption>
        </figure>
    </li>
  {% endfor %}
</ul>